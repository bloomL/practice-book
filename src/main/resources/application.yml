spring:
  datasource:
    url: jdbc:mysql://localhost:3306/bootjpa
    username: root
    password:
    driver-class-name: com.mysql.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
  jackson:
    serialization:
      indent_output: true
server:
  port: 8888
